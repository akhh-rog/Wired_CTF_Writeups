## Baud Boy

This was a hardware CTF task centered on analyzing serial or asynchronous UART data transmission from an Arduino micro-controller.

i was given an arduino and i had saleae logic app
connected the correct pin with the arduino to the arduinos transmittting port

connected the aruiino board to the laptop using a usb wire

using that Measured the width of the narrowest pulse
used a formulaCalculate 
$\text{Baud Rate} = \frac{1}{T}$.

Testing  common baud rates (9600, 115200, 57600, 38400, 19200, 4800, etc.
 found the the flag as when the correct baud rate came it displayed the flag which is attached into this
 ## flag

#### wired{b4d_b0y_0n_th3_d4nc3_fl00r}